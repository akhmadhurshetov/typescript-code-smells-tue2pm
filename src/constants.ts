/**
 * Central place to define magic values for the game.
 * `EMPTY_CELL` and `BOARD_DIMENSION` can be tweaked without touching game logic.
 */
export const EMPTY_CELL = ' ';
export const BOARD_DIMENSION = 3 as const;
